@import "./styles/icontont.wxss";

page, view, text, image, swiper, swiper-item, navigator{
    padding: 0;
    margin: 0;
    box-sizing: border-box;
}

page{
    /* 定义主题颜色变量 */
    --themeColor: #FF0000;

    /* 定义统一字体 */
    font-size: 42rpx;
}

image{
    width: 100%;
}